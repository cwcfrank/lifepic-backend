# Aiven PostgreSQL Connection
DATABASE_URL=postgres://avnadmin:YOUR_PASSWORD@pg-xxx.aivencloud.com:11636/defaultdb?sslmode=require

# TDX API Credentials
TDX_CLIENT_ID=your-client-id
TDX_CLIENT_SECRET=your-client-secret

# Sync API Key (for triggering data sync)
SYNC_API_KEY=your-secure-api-key

# Google Cloud Storage (for feedback image uploads)
GCS_BUCKET_NAME=parkradar
GCS_SERVICE_ACCOUNT_JSON=<base64 encoded service account json>

# SMTP Settings (for feedback email notifications)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USERNAME=no.reply@intvz.com
SMTP_PASSWORD=<SMTP 密碼>
SMTP_RECIPIENTS=service@intvz.com,frank@intvz.com
